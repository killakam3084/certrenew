# Required env for certrenew on TrueNAS.
# Place at /mnt/cell_block_d/apps/certrenew/.env (chmod 600).
# The infisical CLI injects AWS credentials at runtime from the Infisical project.

INFISICAL_TOKEN=<universal-auth-access-token>
INFISICAL_PROJECT_ID=<project-uuid>
INFISICAL_ENV=prod
